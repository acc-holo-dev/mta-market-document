# Быстрый старт

Нужны: Docker, Node 20+, pnpm 9+.

## Сайт

```bash
git clone https://github.com/acc-holo-dev/mta-market-site
cd mta-market-site
cp .env.example .env
# DISCORD_CLIENT_ID / SECRET, JWT_SECRET
docker compose up -d postgres redis
pnpm install
pnpm --filter @mta-market/server contract:emit
pnpm dev
```

Либо `docker compose up -d` со сборкой backend/frontend.

## Модуль

CMake 3.27+, Ninja, компилятор (MinGW/MSVC/GCC), Python 3.11+ для CLI `mta`.

```bash
git clone https://github.com/acc-holo-dev/mta-market-module
cd mta-market-module
# см. other/tools — CLI
mta doctor
mta build
mta test
```

Справка SDK (EN): `other/documents/`. Протокол маркета — этот репозиторий, [03-features/drm.md](../03-features/drm.md).


## PLAN-001 reproducibility checklist (N-001/N-002, verified 2026-09-09)

```sh
# 1. clone + install
git clone https://github.com/acc-holo-dev/mta-market-site && cd mta-market-site
pnpm install

# 2. services: PostgreSQL 16 on :5433 (or :5432 with DATABASE_URL), Redis optional
# 3. migrate database
cd apps/server
npx prisma contract emit && DATABASE_URL=... npx prisma db init   # or: prisma db update

# 4. run
npx tsx watch src/index.ts        # backend :3001
cd ../.. && pnpm --filter @mta-market/web dev   # frontend :3000

# 5. admin account (G-004, no backdoor: refuses NODE_ENV=production)
npx tsx scripts/dev-admin.ts --email admin@dev.local --username admin --password 'dev-password-123'
```

Environment variables: see `apps/server/.env.example` (DATABASE_URL,
JWT_SECRET mandatory; REDIS_URL optional — rate limiter fails open;
YOOKASSA_* optional — dev completion via /payments/:id/simulate;
ARTIFACT_SIGNING_PRIVATE_KEY — Ed25519 PKCS8 base64, generated by
`generatePublisherKeypair()`).

PLAN-001 status: **COMPLETED** (password auth, balance, marketplace cycle,
creation wizard, moderation, full product E2E `tests/plan001-e2e.test.ts`).
